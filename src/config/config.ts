const config = {
    defaults: {
        namespace: 'Application',
    },
    baseUrl: {
        dev: 'https://localhost:5001/',
        pro: 'https://spider.keenneed.com/',
    },
};

export default config;
